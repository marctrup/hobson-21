import { useHuggingFaceCacheReset } from '../hooks/useHuggingFaceCacheReset';

export const HuggingFaceCacheManager = () => {
  useHuggingFaceCacheReset();
  return null; // This component doesn't render anything
};